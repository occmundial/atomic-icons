import Head from 'next/head'
import { useRouter } from 'next/router'
import { Flexbox, Text, Icon, Grid } from '@occmundial/occ-atomic'

export default function NotFound() {
    const router = useRouter()
    return (
        <div>
            
        </div>
    )
}
